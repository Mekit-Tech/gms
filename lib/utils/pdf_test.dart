import 'package:flutter/material.dart';
import 'package:pdf/pdf.dart';

class PdfGeneration extends StatelessWidget {
  const PdfGeneration({super.key});

  @override
  Widget build(BuildContext context) {
    return (Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.white,
      ),
    ));
  }
}
