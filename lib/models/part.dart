class Part {
  String name;
  double cost;
  String category;
  int quantity; // Quantity is optional for parts

  Part({
    required this.name,
    required this.cost,
    required this.category,
    required this.quantity,
  });
}
